# Forge vNext research/integration access audit

Generated: 2026-05-25 19:19:37 CEST
Task: t_133710f0
Scope: discovery only. No posting, account changes, App Store/TestFlight/signing actions, or credential disclosure.

## Executive summary

Forge vNext has strong local execution, browser, Slack MCP, Kanban, and iOS simulator/build automation foundations on this machine. The biggest gaps are public-community dedicated tooling: Reddit/Hacker News/Product Hunt are not configured as first-class CLIs/MCPs; X/Twitter official API tooling is not installed/authenticated; Hermes `x_search` is configured but disabled in the active toolset. For research, prefer read-only browser/web plus Slack MCP and GitHub search today; add dedicated X/Reddit/community tooling only if a lane needs repeatable structured ingestion.

Important worker-environment note: in dispatched Forge verifier runs, `$HOME` is profile-scoped (`/Users/matvii/.hermes/profiles/forgeverifier/home`). Use absolute `/Users/matvii/...` paths when auditing host-level Hermes/Xcode state.

## Capability matrix

| Area | Status | Evidence | Safe command/tool to use | Gaps | Recommended substitute / next step |
| --- | --- | --- | --- | --- | --- |
| Local shell + file access | available | Terminal backend is `local`; workspace is `/Users/matvii/Developer/Personal/forge-e2e-clean`; repo accessible. | `terminal(...)`, `read_file(...)`, `search_files(...)`, `write_file(...)` inside workspace. | None for discovery/build work. | Keep Forge workers in this workspace; avoid `~` for host audit paths because `$HOME` is profile-scoped. |
| Web/search toolset | available in profile | `hermes tools list` shows `web` enabled; config has `web.backend`, `search_backend`, and `extract_backend` empty, meaning default/auto. | Hermes web/search tools in normal sessions; for this worker, use available browser/terminal-safe GETs. | This worker schema did not expose a separate `web_search` function, so web search is profile capability, not directly verified via a web tool call here. | Use browser for ad-hoc public page checks; use a normal Hermes CLI run with web toolset for search-heavy research. |
| Browser automation | available | Browser successfully loaded `https://example.com`; config `browser.engine: auto`; local browser warning says no residential proxy. | `browser_navigate`, `browser_snapshot`, `browser_scroll`, `browser_click`; read-only browsing. | Bot detection may be stronger without residential proxies; private URLs disabled by config. | Use browser for public docs/forums and X URLs when accessible; avoid depending on it for high-volume scraping. |
| X/Twitter via browser | partial / login-gated | `x.com/search?...` redirected to login/onboarding and cookie wall in the current browser session. | Read-only browser navigation to public tweet/status URLs; refuse cookies if prompted; snapshot accessible text. | Search was not accessible logged-out in this session; no logged-in Playwright/X session confirmed. | Use browser for specific public tweet URLs only; for repeatable X search, configure `xurl` or enable a safe X search provider. |
| X/Twitter official CLI (`xurl`) | missing | `xurl` command not found; `/Users/matvii/.xurl` absent; no X/Twitter-related env var names present in worker env. | If installed later: `xurl auth status` only for credential verification; read commands like `xurl search "query" -n 10`. | CLI not installed; credentials not present/safely discoverable. | Install with Homebrew/npm/go if needed; user must configure credentials outside agent session. Do not read `~/.xurl`. |
| Hermes `x_search` toolset | missing / disabled | `hermes tools list` shows `x_search` disabled; config has `x_search.model: grok-4.20-reasoning`. | None in current worker; toolset would need enabling and a fresh session. | Disabled; credentials/provider availability not safely confirmed from env. | Keep disabled unless Forge needs X-native search. Browser + web may be enough for research. |
| Reddit-specific access | missing | No `reddit`, `rdt`, `praw`, or dedicated Reddit CLI found on PATH; no Reddit env var names in worker env; no installed Reddit skill found in profile search. | Browser read-only access to public Reddit pages if needed; generic web search. | No API/CLI/MCP; Reddit frequently rate-limits or blocks automated browsing. | Prefer web search queries targeting Reddit, browser spot-checks, or add a read-only Reddit/public-community collector later. |
| Public-community substitutes | partial | Browser/web available; GitHub CLI available; Slack MCP available; no HN/Product Hunt/Reddit CLIs found (`hn`, `hackernews`, `yt-dlp`, `firecrawl`, `tavily` absent). | Browser/web for Hacker News/Product Hunt/Reddit pages; `gh search`/GitHub CLI for repo/community signals; Slack MCP for internal/product/community discussions. | No dedicated structured community ingestion. | For Forge vNext, define substitutes as evidence with source limitations: browser snapshots, web extracts, GitHub searches, Slack excerpts, and manually curated research notes. |
| Slack local MCP | available | `hermes mcp list` shows `slack` enabled with 9 selected tools; `mcp_slack_channels_me` returned channel rows successfully. | `mcp_slack_channels_me`, `mcp_slack_conversations_search_messages`, `mcp_slack_conversations_history`, `mcp_slack_saved_list`; read-only discovery. | Contains private workspace data; outputs must be summarized carefully and not used as public-community evidence without context. | Use for internal signal and iOS/product feedback. Follow existing preference: local MCP/CLI access, not a Hermes Slack gateway app. |
| Custom Hermes skills relevant to Forge | available | Forge verifier profile has 149 skills; relevant loaded/installed skills include `kanban-worker`, `hermes-agent`, `native-mcp`, `x-twitter`, `xurl`, `xcodebuildmcp-cli`, Slack MCP skills, structured Telegram interviews. | `skill_view(name)` for exact workflow; `skills_list` for discovery. | No dedicated Reddit/public-community skill found. Some relevant skills (xurl) exist but underlying CLI is missing. | Keep using skills as procedural memory; add a Forge research ingestion skill only after a repeatable workflow emerges. |
| Hermes plugins | partial | `hermes plugins list` shows bundled plugins; browser cloud plugins are not enabled; forgeverifier profile has a plugins directory. | `hermes plugins list`; enable only with explicit user decision. | No active plugin identified as necessary for Forge research; Browserbase/Browser Use/Firecrawl plugins not enabled. | Stay with built-in browser/web unless bot detection or extraction reliability becomes a bottleneck. |
| Native MCP framework | available | `mcp_servers.slack` configured in `/Users/matvii/.hermes/profiles/forgeverifier/config.yaml`; native MCP skill loaded; Slack server enabled. | `hermes mcp list`, `hermes mcp test <name>` when needed; direct registered MCP tools. | Only Slack MCP configured for Forge profile. | Add new MCPs only for stable repeated data sources; avoid persisting secrets in config when wrappers can inject runtime env. |
| Xcode command-line tools | partial | `xcodebuild` and `xcrun` exist; global `xcode-select -p` points to `/Library/Developer/CommandLineTools`; bare `xcrun simctl` fails. Full Xcode exists at `/Applications/Xcode-26.5.0.app`. | Use per-command `DEVELOPER_DIR=/Applications/Xcode-26.5.0.app/Contents/Developer ...`; do not change global xcode-select without user approval. | Global developer dir is CLT, so simulator tools fail unless `DEVELOPER_DIR` is set. | In Forge scripts/docs, standardize `DEVELOPER_DIR=/Applications/Xcode-26.5.0.app/Contents/Developer` for sim/build commands. |
| iOS simulator runtimes/devices | available with DEVELOPER_DIR | With `DEVELOPER_DIR=/Applications/Xcode-26.5.0.app/Contents/Developer`, `simctl list devices available` shows iOS 18.2 and iOS 26.5 simulators, including iPhone 17 Pro. | `DEVELOPER_DIR=... xcrun simctl list devices available`; or `xcodebuildmcp simulator list`. | Some older runtimes are unavailable; bare `simctl` not on PATH. | Use iPhone 17 Pro / iOS 26.5 for Forge vNext current simulator checks; iOS 18.2 exists for compatibility. |
| XcodeBuildMCP CLI | available | `xcodebuildmcp` installed under nvm Node; version 2.1.0; `tools --flat` reports 67 canonical / 91 total tools across simulator, device, logging, UI automation, SwiftPM, project discovery, scaffolding. | Prefer `xcodebuildmcp simulator build`, `simulator test`, `simulator snapshot-ui`, `ui-automation screenshot`, `project-discovery discover-projects`. | It still depends on usable Xcode developer dir; set `DEVELOPER_DIR` when sim tooling is needed. | Use XcodeBuildMCP as the default Forge verifier interface for build/run/UI evidence instead of raw `xcodebuild`/`simctl`. |
| Current workspace iOS project | unknown / not present in clean repo root | `*.xcodeproj` search in `/Users/matvii/Developer/Personal/forge-e2e-clean` returned 0; current repo is mostly Forge vNext docs/scripts/fixtures. | `search_files('*.xcodeproj', target='files', path=...)`; `xcodebuildmcp project-discovery discover-projects --workspace-root <app-dir>`. | This clean workspace may not itself contain a buildable generated app. | Treat this repo as Forge pipeline/control-plane; run simulator verification against generated app workspaces/fixtures when produced. |
| Kanban profiles | available | `hermes profile list` shows `default`, `forgedesign`, `forgejudge`, `forgelaunch`, `forgeproduct`, `forgeverifier`; Forge profiles each have config/env and 149 skills. | `hermes profile list`; Kanban worker tools (`kanban_show`, `kanban_create`, `kanban_comment`, `kanban_complete`, `kanban_block`). | Local inspection of `/Users/matvii/.hermes/kanban.db` showed no rows because the active board for this worker is resolved by the Kanban tool/session, not that default DB path. | Use `kanban_*` tools as source of truth inside workers; use profile names above for Forge lane decomposition. |
| Kanban dispatcher/config | available | Forge verifier config: `kanban.dispatch_in_gateway: true`, interval 60s, failure limit 2, stale timeout 14400s. | `kanban_*` tools in workers; human CLI can use `hermes kanban ...`. | Gateway status from profile list shows forgeverifier gateway stopped; default gateway running. Worker was still spawned successfully. | Continue using default/orchestrating gateway for board dispatch; do not shell out to Kanban CLI from workers. |
| App Store Connect/TestFlight/signing | deliberately not checked | Task safety excludes account changes, App Store/TestFlight/signing actions. | None during this audit. | Unknown by design. | For Forge launch lane, keep outputs as local launch packages/checklists unless the user explicitly approves ASC/TestFlight/signing discovery. |

## Recommended Forge vNext research stack now

1. Primary public research: generic web/browser read-only browsing, with source URLs captured in `.forge/research/` notes or evidence index entries.
2. Internal/community signal: Slack MCP search/history/saved-list tools, summarized with privacy discipline.
3. Developer-market signal: GitHub CLI/search and public repo inspection.
4. iOS verification: XcodeBuildMCP with explicit `DEVELOPER_DIR=/Applications/Xcode-26.5.0.app/Contents/Developer`.
5. Evidence policy: when Reddit/X/community content is unavailable through a first-class integration, record it as substitute evidence with source, access method, owner, timestamp, and limitation.

## Highest-value follow-ups

1. Decide whether Forge truly needs repeatable X/Reddit ingestion. If yes, configure `xurl` for X and add a read-only Reddit/community collector; if no, keep using browser/web substitutes.
2. Add a small Forge runner convention for Xcode commands: always set `DEVELOPER_DIR` to the installed full Xcode path unless the user changes global `xcode-select`.
3. Add a pipeline evidence field for `access_method` (`browser`, `web`, `slack_mcp`, `github_cli`, `manual_substitute`, `xcodebuildmcp`) so verifiers can grade research provenance consistently.
4. Avoid App Store/TestFlight/signing discovery until a dedicated launch task explicitly authorizes it.
